# REST & WebSocket API

## WebSocket CLI (Browser Terminal)
Endpoint: `ws://localhost:3000/cli`

## REST Endpoints
- `POST /ask` — Send natural language command
- `GET /status` — Router + system status
- `GET /vouchers` — List active vouchers

Full OpenAPI spec coming soon.
